require 'spec_helper'

describe ForceSslMiddleware do
  it 'has a version number' do
    expect(ForceSslMiddleware::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
